// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
//   swcMinify: true,
// };

// module.exports = nextConfig;

module.exports = {
  images: {
    // for cloudinary -> if image is nopt displayed
    domain: ['res.cloudinary.com'],
  },
};
